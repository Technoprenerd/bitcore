#!/usr/bin/env node

var bitcore = require('bitcore-node-zcoin/lib/cli/bitcore');
var path = require('path');
var servicesPath = path.resolve(__dirname, '../');
var additionalServices = ['insight-api-zcoin', 'insight-ui-zcoin'];
bitcore(servicesPath, additionalServices);
